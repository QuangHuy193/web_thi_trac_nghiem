import classNames from "classnames/bind";

import styles from "./ListExam.module.scss";

const cx = classNames.bind(styles);

function ListExam() {
  return <div>ds bai thi</div>;
}

export default ListExam;
